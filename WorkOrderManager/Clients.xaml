<Page
    x:Class="WorkOrderManager.Clients"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WorkOrderManager"
    xmlns:ClientAppBar="using:WorkOrderManager.ManagementControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
      
      
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="Clients Menu" FontSize="45" HorizontalAlignment="Center" VerticalAlignment="Top" />
        </StackPanel>

        <!--<ClientAppBar:ClientsAppBar x:Name="clientAppBar" Grid.Row="1" VerticalAlignment="Bottom" />-->
    </Grid>
</Page>
